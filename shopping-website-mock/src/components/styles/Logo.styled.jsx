import styled from 'styled-components';

export const StyledLogo = styled.div`

    padding-left: 20px;

    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-around;
    gap: 10px;

    h1 {
        font-size: 50px;
        margin: 0;
        padding: 10px 0;
    }
`